# ============================================
# KUBERNETES SECRETS (Encrypted)
# Use: kubectl create secret generic
# ============================================

# DON'T STORE SECRETS IN PLAIN TEXT!
# Use this command to create secrets:

# kubectl create secret generic ai-sentry-secrets \
#   --namespace security \
#   --from-literal=database-url='postgresql://user:password@postgres:5432/ai_sentry' \
#   --from-literal=redis-url='redis://redis:6379/0' \
#   --from-literal=slack-webhook='https://hooks.slack.com/services/XXX/YYY/ZZZ' \
#   --from-literal=pagerduty-key='your-pagerduty-key'

# For production, use sealed-secrets or external secrets operator